import { FunctionComponent, ReactElement } from "react";
import { Box } from "@chakra-ui/react";

export const Home: FunctionComponent = (): ReactElement => {
  return <Box>Welcome to the mfe2</Box>;
};
